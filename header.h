#ifndef HEADER_H_ // This is just a guard to avoid multiple inclusions of the same header file
#define HEADER_H_ // this line will execute only if above above condition is failed, so the header file is included only once

//Function protoype
int getPassedNumber(int number);

#endif // endif for #ifndef HEADER_H_